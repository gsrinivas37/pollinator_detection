class Classifier:
    def __int__(self, model_path, type):
        self.model_path = self.model_path
        self.type = type  # ResNet or VGG

    def run(self, image):
        """
        Args:
            image: Path to the image file

        Returns:
            Whether pollinator exists in the image or not
        """
        return True
